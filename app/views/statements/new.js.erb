var element = $("<%= escape_javascript(render :partial => 'statements/new') %>").statement({'level' : <%= @node_environment.level %>});

/* Breadcrumb's stack (if follow-up question */
<% if @breadcrumb %>
   $('#breadcrumbs').data('breadcrumbApi').addBreadcrumbs(<%= [@breadcrumb].map(&:to_hash).to_json %>);
<% end %>

